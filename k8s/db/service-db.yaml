apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  type: NodePort
  # clusterIP: None
  ports:
  - port: 5432
    nodePort: 32000
    # name: postgres
  selector:
    app: postgres